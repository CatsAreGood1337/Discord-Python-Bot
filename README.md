#Discord Bot

A basic discord bot!

## Setup
`pip install -r requirements.txt`

## Start
`python main.py`

## Config
Make a file called `.env` and type the following
```
TOKEN: (Your token)
PREFIX: (The prefix)
```



